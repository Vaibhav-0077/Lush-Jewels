<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Lush Jewels - Rings Collection</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css">
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body {
      font-family: 'Georgia', serif;
      color: #333;
    }
    .product-card:hover {
      transform: translateY(-10px);
      transition: transform 0.3s ease;
    }
    .modal {
      display: none;
      position: fixed;
      z-index: 1;
      left: 0;
      top: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(0, 0, 0, 0.9);
      justify-content: center;
      align-items: center;
    }
    .animate-fade-in {
      animation: fadeIn 1s ease-in-out;
    }
    @keyframes fadeIn {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }
    
  </style>
</head>
<body class="bg-gray-50">
  <!-- Header -->
  <header
      class="sticky top-0 z-50 shadow-md h-22"
      style="background-color: #f8e8ee;"
    >
    <div class="container mx-auto px-6 py-1">
      <div class="flex items-center">
        <!-- <div class="text-2xl font-bold text-gray-800 flex-shrink-0">Lush Jewels</div> -->
          <div class="flex items-center h-full flex-shrink-0">
            <a href="/" class="h-full flex items-center">
              <img
                src="/images/lush_jewels_header_logo.png"
                alt="Lush Jewellery Logo"
                class="h-20 w-auto object-contain"
              />
            </a>
          </div>

          
        <!-- Center: Search Box (hidden on small screens) -->
        <div class="flex-grow flex justify-center hidden lg:flex mx-6">
          <input type="text" id="header-search-input" placeholder="Search jewelry..."
            class="w-full max-w-md px-3 py-2 border border-gray-300 rounded-l-full focus:outline-none focus:ring-2 focus:ring-gray-800"
            aria-label="Search jewelry" />
          <button id="header-search-btn"
            class="bg-gray-800 text-white px-4 py-2 rounded-r-full hover:bg-gray-700 transition whitespace-nowrap"
            aria-label="Search">
            Search
          </button>
        </div>

        <!-- Right: Navigation Menu -->
        <!-- Right: Navigation Menu -->
        <nav class="hidden lg:flex space-x-6 flex-shrink-0">
          <a href="#home" class="text-gray-600 hover:text-gray-900">Home</a>

          <% if (session && session.user) { %>
            <a href="/user/account" class="text-gray-600 hover:text-gray-900 ml-4">My Account</a>
          <% } else { %>
            <a href="/user/login" class="text-gray-600 hover:text-gray-900 ml-4">Login</a>
          <% } %>

          <a href="#products" class="text-gray-600 hover:text-gray-900 ml-4">Products</a>
          <a href="#contact" class="text-gray-600 hover:text-gray-900 ml-4">Contact</a>



          <!-- Cart Button -->
          <button id="cart-btn" class="relative bg-gray-800 text-white px-4 py-2 rounded hover:bg-gray-700">
            🛒 Cart
            <span id="cart-count"
              class="absolute -top-2 -right-2 bg-red-500 text-white text-xs px-2 py-0.5 rounded-full">0</span>
          </button>
        </nav>

        <!-- Mobile Menu Button -->
        <div class="lg:hidden ml-auto">
          <button id="menu-btn" class="text-gray-600 focus:outline-none" aria-label="Toggle menu">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor"
              class="w-6 h-6">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7" />
            </svg>
          </button>
        </div>
      </div>

      <!-- Mobile Menu -->
      <nav id="mobile-menu" class="lg:hidden hidden px-6 pb-4 space-y-2 bg-#f8e8ee shadow-md">
        <!-- Mobile Search Box -->
        <div class="flex py-2">
          <input type="text" id="mobile-search-input" placeholder="Search jewelry..."
            aria-label="Search jewelry" />
          <button id="mobile-search-btn"
            class="bg-gray-800 text-white px-4 py-2 rounded-r-full hover:bg-gray-700 transition" aria-label="Search">
            Search
          </button>
        </div>
        <a href="#home" class="block text-gray-600 py-2">Home</a>

        <% if (session && session.user) { %>
          <a href="/user/account" class="block text-gray-600 hover:text-gray-900 py-2">My Account</a>
        <% } else { %>
          <a href="/user/login" class="block text-gray-600 hover:text-gray-900 py-2">Login</a>
        <% } %>

        <a href="#products" class="block text-gray-600 py-2">Products</a>
        <a href="#contact" class="block text-gray-600 py-2">Contact</a>
            <!-- Cart Button for Mobile -->
            <button id="mobile-cart-btn"
            class="relative bg-gray-800 text-white px-4 py-2 rounded hover:bg-gray-700 w-full text-left">
              🛒 Cart
              <span id="mobile-cart-count"
                class="absolute top-1/2 -translate-y-1/2 right-2 bg-red-500 text-white text-xs px-2 py-0.5 rounded-full">0</span>
            </button>
      </nav>
    </div>
  </header>

  <!-- Rings Collection Section -->
  <section class="py-16 px-6 bg-gray-100" style="background: linear-gradient(135deg, #f8e8ee, #fdecef);">
    <div class="container mx-auto">
      <h2 class="text-4xl font-bold text-center mb-8">Rings Collection</h2>
      <p class="text-lg text-center mb-12 max-w-2xl mx-auto">Timeless ring designs from engagement gems to everyday
         elegance, crafted with premium metals and stones.</p>

      
<!-- ============================================================================================================================================ -->
      <section id="products" class="py-16 px-6 bg-gray-100" style="background: linear-gradient(135deg, #f8e8ee, #fdecef);">
        <div class="container mx-auto">
          <h2 class="text-3xl font-bold text-center mb-10">Our Collections</h2>

          <% if (products && products.length> 0) { %>
            <!-- Full-width grid: 4 boxes per row, no extra space -->
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 justify-items-stretch">
              <% products.forEach(p=> { %>
                <div
                  class="product-card bg-white rounded-xl shadow-lg p-6 hover:shadow-2xl transition-all duration-300 w-full"
                  data-id="<%= p._id %>">
                  <img src="<%= p.img.startsWith('http') ? p.img : ('/' + p.img) %>" alt="<%= p.name %>"
                    class="w-full h-80 object-cover rounded-lg"
                    onerror="this.style.display='none'; this.parentElement.style.backgroundColor='#f0f0f0'" />
                  <h3 class="text-xl font-semibold mt-4 text-gray-800">
                    <%= p.name %>
                  </h3>
                  <p class="text-gray-600 text-lg mb-3">₹<%= p.price %>
                  </p>
                  <button
                    class="view-btn relative w-full mt-2 px-[60px] py-4
                          rounded-full
                          text-white text-[16px] font-bold tracking-[1px]
                          cursor-pointer
                          bg-[linear-gradient(135deg,#d9828c_0%,#d9828c_20%,#d9828c_60%,#d9828c_100%)]
                          
                          shadow-[inset_0_2px_6px_rgba(255,255,255,0.8),inset_0_-4px_8px_rgba(180,80,90,0.35),0_12px_25px_rgba(215,120,130,0.45)]
                          
                          transition-all duration-300 ease-in-out
                          hover:-translate-y-0.5
                          hover:shadow-[inset_0_2px_6px_rgba(255,255,255,0.9),inset_0_-4px_10px_rgba(180,80,90,0.45),0_16px_30px_rgba(215,120,130,0.6)]
                        "
                      >
                    <!-- Inner white border -->
                    <span
                      class="pointer-events-none absolute inset-[6px] rounded-full border-[3px] border-white/90">
                    </span>

                    View Details
                  </button>
                </div>
                <% }) %>
            </div>
            <% } else { %>
              <p class="text-center text-gray-500">No products available.</p>
              <% } %>
        </div>
      </section>




<!-- ============================================================================================================================================ -->



      <div id="back-all" class="text-center mt-8">
        <button onclick="window.location.href='/'" class="bg-gray-800 text-white px-6 py-3 rounded-full hover:bg-gray-700 transition">
          Back to All Collections
      </div>
    </div>
  </section>

  <!-- Modal -->
  <div id="product-modal" class="modal">
    <div class="bg-white p-6 rounded-lg max-w-md w-full">
      <span id="close-modal" class="float-right text-2xl cursor-pointer">&times;</span>
      <div id="modal-content"></div>
    </div>
  </div>

<!-- ✅ Sidebar Cart -->
  <div id="cart-modal"
    class="fixed top-0 right-0 h-full w-80 max-w-full bg-white shadow-lg z-50 transform translate-x-full transition-transform duration-300 md:w-96 flex flex-col">

    <!-- Header -->
    <div class="flex items-center justify-between p-4 border-b">
      <h2 class="text-xl font-bold">Your Cart</h2>
      <button id="close-cart" class="text-2xl">&times;</button>
    </div>

    <!-- Cart Items -->
    <div id="cart-items" class="flex-1 overflow-y-auto p-4 space-y-3">
      <!-- JS will inject items here -->
    </div>

    <!-- Cart Footer (now sticks to bottom) -->
    <div class="border-t p-4">
      <div class="flex justify-between mb-3">
        <span class="font-semibold">Total:</span>
        <span id="cart-total" class="font-bold">₹0</span>
      </div>
      <button id="checkout-btn" class="bg-green-600 text-white px-4 py-2 rounded w-full">
        Checkout
      </button>
    </div>
  </div>



  <!-- ✅ Checkout Modal -->
  <div id="checkout-modal" class="fixed inset-0 z-50 hidden items-center justify-center bg-black bg-opacity-50">
    <div class="bg-white p-6 rounded-lg max-w-md w-full relative">
      <span id="close-checkout" class="absolute top-2 right-4 text-2xl cursor-pointer">&times;</span>
      <h2 class="text-2xl font-bold mb-4">Checkout</h2>
      <form id="checkout-form" class="space-y-4">
        <input type="text" id="name" placeholder="Full Name" class="w-full border px-3 py-2 rounded" required>
        <input type="text" id="address" placeholder="Address" class="w-full border px-3 py-2 rounded" required>
        <input type="tel" id="phone" placeholder="Phone Number" class="w-full border px-3 py-2 rounded" required>

        <div>
          <label class="block font-semibold mb-2">Payment Method:</label>
          <input type="radio" id="cod" name="payment" value="COD" checked>
          <label for="cod">Cash on Delivery</label>
        </div>

        <button type="submit" class="bg-green-600 text-white px-4 py-2 rounded w-full">
          Confirm Order
        </button>
      </form>
    </div>
  </div>


  <!-- Toast -->
  <div id="toast" class="fixed top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 
          bg-black text-white px-4 py-2 rounded-lg shadow-lg hidden z-50 text-lg font-semibold">
  </div>


  <%- include('partials/cart') %>

    <div
      id="toast"
      class="hidden fixed top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2
      bg-gray-900 text-white px-6 py-3 rounded-lg shadow-lg z-[9999]
      text-base transition-all duration-500"
    ></div>



  <!-- Footer -->
    <!-- Footer -->
  <footer id="contact" class="bg-gray-900 text-gray-300 pt-12 pb-6">
  <div class="max-w-7xl mx-auto px-6 grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-8">

     <!-- 🛍️ Brand + About -->
    <div>
        <h1 class="text-2xl md:text-3xl font-extrabold text-white mb-4 tracking-wide">
          Lush Jewels
        </h1>

        <a href="/">
        <img
          src="/images/lush_jewels_logo.png"
          alt="Lush Jewellery Logo"
          class="h-20 sm:h-28 md:h-32 ml-11  object-contain"
        />
      </a>

    </div>

    <!-- 💎 Shop Links -->
    <div>
      <h4 class="text-lg font-semibold text-white mb-3">Shop</h4>
      <ul class="space-y-2 text-sm">
        <li><a href="/category/necklaces" class="hover:text-yellow-400 transition">Necklaces</a></li>
        <li><a href="/category/earrings" class="hover:text-yellow-400 transition">Earrings</a></li>
        <li><a href="/category/rings" class="hover:text-yellow-400 transition">Rings</a></li>
        <li><a href="/category/bracelets" class="hover:text-yellow-400 transition">Bracelets</a></li>
      </ul>
    </div>

    <!-- 📞 Customer Support -->
    <div>
      <h4 class="text-lg font-semibold text-white mb-3">Customer Service</h4>
      <ul class="space-y-2 text-sm">
        <li><a href="/help-center" class="hover:text-yellow-400 transition">Help Center</a></li>
        <li><a href="/shipping-policy" class="hover:text-yellow-400 transition">Shipping & Returns</a></li>
        <li><a href="/privacy-policy" class="hover:text-yellow-400 transition">Privacy Policy</a></li>
        <li><a href="/terms" class="hover:text-yellow-400 transition">Terms & Conditions</a></li>
        <li><a href="/refund-policy" class="hover:text-yellow-400 transition">Refund Policy</a></li>
      </ul>
    </div>


    <!-- 📱 Contact Info -->
    <div>
      <h4 class="text-lg font-semibold text-white mb-3">Contact Us</h4>
      <p class="text-sm">📧  lushjewellery07@gmail.com</p>
      <p class="text-sm mb-4">📞 +91 1234567899</p>
      <div class="flex space-x-4 mt-2">
        <a href="#" class="hover:text-yellow-400"><i class="fab fa-facebook-f"></i></a>
        <a href="#" class="hover:text-yellow-400"><i class="fab fa-instagram"></i></a>
        <a href="#" class="hover:text-yellow-400"><i class="fab fa-twitter"></i></a>
        <a href="#" class="hover:text-yellow-400"><i class="fab fa-youtube"></i></a>
      </div>
    </div>

  </div>

  <!-- Divider -->
  <div class="border-t border-gray-700 mt-10 pt-4 text-center text-sm text-gray-500">
    © 2025 <span class="font-semibold text-yellow-400">Lush Jewels</span>. All rights reserved.
  </div>
</footer>

<!-- Font Awesome for Social Icons -->
<script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>



<script>
document.addEventListener("DOMContentLoaded", () => {
  // ================================
  // 🛒 CART OPEN/CLOSE FUNCTIONALITY
  // ================================
  const cartBtn = document.getElementById("cart-btn");
  const cartModal = document.getElementById("cart-modal");
  const closeCart = document.getElementById("close-cart");
  const checkoutBtn = document.getElementById("checkout-btn");

  if (cartBtn && cartModal && closeCart) {
    cartBtn.addEventListener("click", () => {
      renderCart();
      cartModal.classList.remove("translate-x-full");
    });

    closeCart.addEventListener("click", () => {
      cartModal.classList.add("translate-x-full");
    });
  }


  // ===================================
  // 🛍️ ADD TO CART BUTTON FUNCTIONALITY
  // ===================================
  document.querySelectorAll(".add-to-cart").forEach((btn) => {
    btn.addEventListener("click", () => {
      const item = {
        name: btn.dataset.name,
        price: parseFloat(btn.dataset.price),
        img: btn.dataset.img,
        qty: 1,
      };

      const existing = cart.find((p) => p.name === item.name);
      if (existing) {
        existing.qty += 1;
      } else {
        cart.push(item);
      }

      saveCart();
      showToast("✅ Added to cart");
    });
  });

  // ===================================
  // 💳 PROCEED TO CHECKOUT
  // ===================================
    // 🔥 Toast Utility
document.addEventListener("DOMContentLoaded", () => {
  const checkoutBtn = document.getElementById("checkout-btn");

  // 🧠 Toast function
  function showToast(message, type = "success") {
    let toast = document.getElementById("toast");
    if (!toast) {
      toast = document.createElement("div");
      toast.id = "toast";
      document.body.appendChild(toast);
    }

    toast.textContent = message;
    toast.className = `
      fixed bottom-6 left-1/2 transform -translate-x-1/2
      z-50 px-5 py-3 rounded-lg shadow-lg text-white text-base font-medium
      opacity-0 transition-opacity duration-300
      ${type === "error" ? "bg-red-600" : "bg-green-600"}
    `;

    requestAnimationFrame(() => toast.classList.add("opacity-100"));
    setTimeout(() => {
      toast.classList.remove("opacity-100");
      setTimeout(() => toast.remove(), 300);
    }, 2000);
  }

  // 🔒 Checkout protection (login check)
  if (checkoutBtn) {
    checkoutBtn.addEventListener("click", (e) => {
      e.preventDefault();

      const isLoggedIn = "<%= session && session.user ? 'true' : 'false' %>";

      if (isLoggedIn === "true") {
        window.location.href = "/checkout";
      } else {
        showToast("⚠ Please login to proceed to checkout.", "error");
        setTimeout(() => {
          window.location.href = "/user/login?message=Please login to continue";
        }, 1500);
      }
    });
  }
});


  // ===================================
  // 🔔 CENTERED TOAST MESSAGES
  // ===================================
  function showToast(message, type = "success") {
    let toast = document.getElementById("toast");
    if (toast) toast.remove();

    toast = document.createElement("div");
    toast.id = "toast";
    toast.textContent = message;
    toast.className = `fixed top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2
      z-50 px-6 py-3 rounded-xl shadow-lg text-white text-lg ${
        type === "error" ? "bg-red-600" : "bg-green-600"
      }`;
    document.body.appendChild(toast);
    setTimeout(() => toast.remove(), 2000);
  }
});



document.querySelectorAll(".view-btn").forEach((btn) => {
  btn.addEventListener("click", () => {
    const card = btn.closest(".product-card");
    const id = card.getAttribute("data-id");
    if (id) {
      window.location.href = `/product/${id}`;
    } else {
      alert("❌ Product ID not found!");
    }
  });
});
</script>

<!-- Global JS for cart, search, toast, mobile menu -->
<script src="/script.js"></script>




</body>
</html>